name: 変更のあったファイルを翻訳しなおす

on:
  # "main" branch に変更があった場合にトリガする
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # マニュアル実行
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: echo
        run: echo Hello, world!
